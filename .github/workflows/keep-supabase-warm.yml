name: Keep Supabase Warm

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

jobs:
  warmup:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Call warmup endpoint
        env:
          WARMUP_ENDPOINT: ${{ secrets.WARMUP_ENDPOINT }}
          WARMUP_TOKEN: ${{ secrets.WARMUP_TOKEN }}
        run: |
          if [ -z "$WARMUP_ENDPOINT" ] || [ -z "$WARMUP_TOKEN" ]; then
            echo "Missing WARMUP_ENDPOINT or WARMUP_TOKEN secret."
            exit 1
          fi

          curl --fail --show-error --silent \
            --header "x-warmup-token: $WARMUP_TOKEN" \
            "$WARMUP_ENDPOINT"
